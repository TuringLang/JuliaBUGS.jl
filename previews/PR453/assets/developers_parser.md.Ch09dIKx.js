import{c as r,S as t,j as a,m as o}from"./chunks/framework.EDvvEH7Q.js";const u=JSON.parse('{"title":"BUGS Parser","description":"","frontmatter":{},"headers":[],"relativePath":"developers/parser.md","filePath":"developers/parser.md","lastUpdated":null}'),s={name:"developers/parser.md"};function n(i,e,l,c,p,h){return t(),a("div",null,[...e[0]||(e[0]=[o('<h1 id="BUGS-Parser" tabindex="-1">BUGS Parser <a class="header-anchor" href="#BUGS-Parser" aria-label="Permalink to &quot;BUGS Parser {#BUGS-Parser}&quot;">​</a></h1><p>The macro <a href="/JuliaBUGS.jl/previews/PR453/two_macros#@bugs"><code>@bugs</code></a> produces a Julia <code>Expr</code> object that represents the BUGS model definition.</p><p>If the input is a <code>String</code>, it&#39;s assumed to be a program in the original BUGS language. In this case, the macro will first convert the program to an equivalent Julia program, then use the Julia parser to parse the program into an <code>Expr</code> object.</p><p>Both model definitions written in Julia and those written in the original BUGS and subsequently parsed are now represented as a Julia <code>Expr</code> object. These objects go through syntax checking and post-processing to create the input for the <code>compile</code> function.</p><p>Below, we describe how the original BUGS program is translated to an equivalent Julia program and detail the post-processing done to the <code>Expr</code> object.</p><h2 id="BUGS-to-Julia-Translation" tabindex="-1">BUGS to Julia Translation <a class="header-anchor" href="#BUGS-to-Julia-Translation" aria-label="Permalink to &quot;BUGS to Julia Translation {#BUGS-to-Julia-Translation}&quot;">​</a></h2><p>In this section, we refer to the translation program as the &quot;parser&quot; and the translating process as &quot;parsing&quot;. Although the parser doesn&#39;t produce a syntax tree, it does follow the form of a recursive descent parser, building a Julia program in the form of a vector of tokens rather than a syntax tree.</p><p>This general implementation is heavily inspired by <a href="https://github.com/JuliaLang/JuliaSyntax.jl" target="_blank" rel="noreferrer"><code>JuliaSyntax.jl</code></a>, the official parser for Julia since version 1.10.</p><p>The BUGS parser implemented here takes a token stream with a recursive descent structure and checks the program&#39;s correctness. Here&#39;s how it works:</p><ol><li><p>Use <a href="https://julialang.github.io/JuliaSyntax.jl/dev/api/#JuliaSyntax.tokenize" target="_blank" rel="noreferrer"><code>tokenize</code></a> to obtain the token vector.</p></li><li><p>Inspect the tokens and build the Julia version of the program as a vector of tokens.</p></li><li><p>Push the token to the Julia version of the program vector when appropriate.</p></li><li><p>Detect errors and make necessary alterations to tokens, such as deletion, combination, or replacement.</p></li></ol><p>During the recursive descent, BUGS syntax tokens will be translated into Julia syntax tokens. Some tokens will remain as they are, while others will be transformed, removed, or new tokens may be added.</p><p>The parser will throw an error if it encounters a program that does not adhere to strict BUGS syntax.</p><h3 id="Some-Notes-on-Error-Recovery" tabindex="-1">Some Notes on Error Recovery <a class="header-anchor" href="#Some-Notes-on-Error-Recovery" aria-label="Permalink to &quot;Some Notes on Error Recovery {#Some-Notes-on-Error-Recovery}&quot;">​</a></h3><p>The current error recovery is ad hoc and primarily rudimentary. If the program is correct, it will produce the correct result. If the program is syntactically or semantically incorrect, the token stream will not be pushed forward, resulting in failure.</p><p>The failure detection mechanism checks if two errors occur with the same &quot;current token&quot;. If they do, the parser stops and reports the error. This ensures that the parser won&#39;t incorrectly parse a flawed program.</p>',15)])])}const m=r(s,[["render",n]]);export{u as __pageData,m as default};
